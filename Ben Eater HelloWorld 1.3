Assembly language <===> Machine code 
             VASM 
portable and retargetable assembler 

Download "Source code" from "Last release source"

# 1  
Command on CMD 
  make CPU=6502 SYNTAX=oldstyle 
  vasm6502_oldstyle blink.s                 (Text file)
  cat a.out  
  vasm6502_oldstyle -Fbin blink.s           (Binary file) 
  cat a.out 
  hexdump -C a.out                          (Check the contents)
  hexdump -C rom.bin                        (Indentify what is in EEPROM)
  vasm6502_oldstyle -Fbin -dotbir blink.s   (.word)
  minipro -p AT28C256 -w a.out                      (Erase EEPROM)
  vasm6502_oldstyle -Fbin -dotbir hello-world.s     (Save to EEPROM)
  

# 2  
Packaging Machine code ===> Assembly language : 
  .org $8000
reset :
  lda #$ff    
  sta $6002
  lda #$55    
  sta $6000
loop:
  ror           (sta #$aa) Shifting pattern to right and rotating    
  sta $6000
  jump loop     ($8005) 

  .org $fffc
  .word reset   ($8000)
  .word $0000



